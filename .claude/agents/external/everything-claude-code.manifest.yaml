---
schema_version: 1.0.0
framework: everything-claude-code
source: https://github.com/mrjefftang/everything-claude-code.git
ref: main
paths:
  agents: .claude/agents
sync_strategy: sparse-checkout
imported_agents:
  - code-reviewer
  - librarian
  - validator
  - worker
---

# everything-claude-code Agent Manifest

本文件声明 Lincoln 需要引用的 everything-claude-code 框架 agent。

运行 `scripts/sync-external-agents.sh` 会根据本 manifest 把指定 agent 拉取到 `.claude/agents/external/everything-claude-code/`。

这些 agent 供 Lincoln 自有角色通过 `extends` 字段组合使用。
